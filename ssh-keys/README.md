# SSH public keys

Place the public keys that Ansible should install on hosts in this directory.
Only public keys (for example, files ending in `.pub`) may be committed here.
Never store private keys or bootstrap passwords in this directory.
