# Every apps/<dir> with a compose file becomes a stack, including doco-cd.
name: apps
working_dir: apps
auto_discovery:
  enabled: true
  depth: 1
  delete: true
  remove_volumes: false
  remove_images: true
