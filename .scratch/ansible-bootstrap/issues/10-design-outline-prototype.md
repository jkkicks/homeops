# Design outline prototype

Type: prototype
Status: resolved
Blocked by: 09

Part of: [.scratch/ansible-bootstrap/map.md](../map.md)

## Question

Does a rough outline of the design doc (and a sketched inventory/playbook tree, not working Ansible) match the intended shape well enough to lock the deliverable and close the map’s remaining fog into the written plan?

## Answer

**Yes.** The throwaway outline at [prototype-design-outline.md](../prototype-design-outline.md) matches the intended shape for `docs/ansible-bootstrap.md` (modes, secrets, inventory sketch, sequences, make/`ansible/` tree, docs migration).

Remaining fog / open tickets (11 quorum, 12 setup-key ops, 13 day-N rename, Netbird ACLs, Docker pin, exact `docs/` subdirs) still need resolution before the real design doc is considered complete — they fold into that doc when closed, they do not change the outline’s skeleton.
