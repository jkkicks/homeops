---
- name: Validate SSH configuration
  ansible.builtin.command:
    cmd: /usr/sbin/sshd -t
  changed_when: false
  listen: Apply SSH lockdown

- name: Reload SSH service
  ansible.builtin.systemd_service:
    name: ssh
    state: reloaded
  listen: Apply SSH lockdown
